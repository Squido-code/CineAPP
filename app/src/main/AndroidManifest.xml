<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.guillermo.cine">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Peliculas"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".login.view.LoginView"
            android:label="@string/title_activity_login_view"
            android:theme="@style/Theme.Peliculas.NoActionBar"></activity>
        <activity android:name=".fichaDescriptiva.view.FichaDescriptiva"></activity>
        <activity android:name=".listaPeliculas.view.ViewListaPeliculas">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>